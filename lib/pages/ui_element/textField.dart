import 'package:flutter/material.dart';

class TextFieldElement extends StatelessWidget{
  final String type;

  TextFieldElement(this.type);

  @override
  Widget build(BuildContext context) {

    return TextFormField(

    );
  }
}